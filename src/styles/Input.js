import styled from "styled-components";

const Input = styled.input`
  padding: 0.5rem 1rem;
  min-width: 250px;
  background-color: var(--bg-nemesis);
  border: 1px solid var(--bg-nemesis);
  border-radius: 4px;
  color: var(--primaryColor);
  margin: 1.5rem 0;
  margin-bottom: 1rem;
`;

export default Input;
